# browser config
browser=chrome
browserVersion=127.0
browserSize=1920x1080

# base url/uri config
baseUrl=https://t-j.ru

# test run remote/local environment
isRemote=true
selenoidRemoteHost=selenoid.autotests.cloud
selenoidLogin=${selenoidLogin}
selenoidPassword=${selenoidPassword}